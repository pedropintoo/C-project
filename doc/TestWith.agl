# Deve dar erro pois o Rectangle não é assim que se defined
# Ainda não dá erro. Falta implementar os atributos.

a: Rectangle with {
    width = 100;
    height = 200;
}

with pacman do {
    a = 1;
}